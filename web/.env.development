# Loaded automatically by Vite in development (`npm run dev`).
# Pubkey only — safe to commit; override in `.env.local` if needed.
VITE_DEV_WALLET_PUBKEY=8s9BcTUTXmWmZVPDrkoMNKsU6n1dRsihySv1bSteSvMQ

# Vite proxy default (see `vite.config.ts`): `/api` → http://127.0.0.1:${API_PORT}
API_PORT=8081
