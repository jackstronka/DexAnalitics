# Copy to `.env` or `.env.local` in this directory (both are gitignored by root `.env` rule).
# Restart `npm run dev` after changes.
#
# Proxy `/api` → `API_UPSTREAM` or `http://127.0.0.1:${API_PORT}`.
# Vite merges repo root `.env` — default dev port is **8081** (see `web/.env.development` and `vite.config.ts`).
# If you only run `npm run dev` + API on another port, set `API_PORT` or `API_UPSTREAM` here or in root `.env`.

# Solana pubkey (base58) — shown in the shell as the pinned test/operator wallet.
# Default for dev is also in `.env.development` (committed).
VITE_DEV_WALLET_PUBKEY=8s9BcTUTXmWmZVPDrkoMNKsU6n1dRsihySv1bSteSvMQ

# Optional: disable WebSockets in UI (avoids Vite ws proxy spam during frequent API restarts)
VITE_DISABLE_WS=0

# Optional: Bearer token for POST …/chain-history/refresh from the UI (must match API CLMM_CHAIN_HISTORY_REFRESH_SECRET when that env is set).
# VITE_CHAIN_HISTORY_REFRESH_SECRET=
